# Apple Silicon only — local GPU transcription via MLX.
# Install alongside the base requirements:
#   pip install -r requirements.txt -r requirements-mac.txt
mlx-whisper
# Also provides the bundled Silero VAD the mlx backend cuts clips with.
faster-whisper
