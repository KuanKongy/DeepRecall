# The Railway public domain (Railway → api service → Settings → Networking →
# Generate Domain). Committed on purpose: it is a public URL, not a secret.
VITE_API_URL=https://deeprecall.railway.internal
